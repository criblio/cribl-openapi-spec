overlay: 1.0.0
info:
  title: Overlays for control-plane spec
  version: 0.0.1
actions:
  - target: $["paths"]["/auth/login"]["post"]["responses"]["429"]["headers"]
    description: Overlay to fix duplicate example in Retry-After header after filtering (GEN-2002) for Speakeasy CLI bug. Remove once fixed.
    remove: true
  - target: $["components"]["schemas"]["OutputChronicle"]["properties"]["authenticationMethod"]["not"]
    description: Overlay to not supported not in enum property (PLAT-7942). Remove this with 4.15.0 release.
    remove: true
