overlay: 1.0.0
info:
  title: Overlay to fix spec validation errors (PLAT-7942)
  version: 0.0.1
actions:
  - target: $["components"]["schemas"]["OutputChronicle"]["properties"]["authenticationMethod"]["not"]
    description: Remove this with 4.15.0 release.
    remove: true
